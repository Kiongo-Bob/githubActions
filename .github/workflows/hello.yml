name: hello

on: 
  push: 
    branches: [main]
jobs: 
 log-hello:
   runs-on: ubuntu-latest
   steps: 
    - uses: actions/checkout@v3
    - name: console log <Hello world!> using Nodejs
    - uses: actions/setup-node@v3
      with: 
        node-version: '18.x'
    - name: install dependencies
    - run: npm install
    - run: index.js
